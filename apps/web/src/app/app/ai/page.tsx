import { RoadmapPage } from '@/components/roadmap-page';

export default function AiPage() {
  return (
    <RoadmapPage
      title="Family Copilot"
      phase="Fase 6 — AI"
      description="O AI Gateway (packages/ai) já impõe a regra estrutural mais importante: nenhuma pergunta chega ao modelo de linguagem sem passar pelo Family Policy Engine. A interface de chat chega nesta fase."
    />
  );
}
