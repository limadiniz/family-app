import { RoadmapPage } from '@/components/roadmap-page';

export default function DocumentsPage() {
  return (
    <RoadmapPage
      title="Documentos"
      phase="Fase 4 — Health / Fase 5 — School"
      description="Upload seguro com storage privado, URLs assinadas e pipeline de OCR com confirmação humana antes de qualquer dado extraído virar verdade no sistema."
    />
  );
}
