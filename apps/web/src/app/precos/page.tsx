import { MarketingPage } from '@/components/marketing-page';

export default function PrecosPage() {
  return (
    <MarketingPage title="Preços">
      <p>
        O Family App está em desenvolvimento (fase de fundação técnica). A estrutura de preços será definida antes
        do lançamento público — esta página é um placeholder reversível, não uma decisão comercial final.
      </p>
    </MarketingPage>
  );
}
